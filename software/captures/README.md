# Captures

Track capture metadata, file manifests, and small excerpts here.

Do not commit large raw capture files. Keep those in external storage and reference them with:
- capture date
- location
- setup revision
- antenna geometry
- software/firmware versions
